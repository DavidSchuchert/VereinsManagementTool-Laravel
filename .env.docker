# .env.docker - Database configuration for Docker development environment

DB_CONNECTION=mysql
DB_HOST=mariadb
DB_PORT=3306
DB_DATABASE=vereinsmanagement
DB_USERNAME=vereinsuser
DB_PASSWORD=vereinspass

# This .env.docker file is for Docker use only. Do not commit to version control.
